apiVersion: apps/v1
kind: Deployment
metadata:
  name: fees
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fees
  template:
    metadata:
      labels:
        app: fees
    spec:
      containers:
        - name: fees
          image: rvtrust/fees:latest
          envFrom:
            - secretRef:
                name: fees-secrets
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: fees
spec:
  selector:
    app: fees
  ports:
    - port: 80
      targetPort: 3000
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: fees-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: fees
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: fees-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: fees
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
