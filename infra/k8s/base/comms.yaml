apiVersion: apps/v1
kind: Deployment
metadata:
  name: comms
spec:
  replicas: 2
  selector:
    matchLabels:
      app: comms
  template:
    metadata:
      labels:
        app: comms
    spec:
      containers:
        - name: comms
          image: rvtrust/comms:latest
          envFrom:
            - secretRef:
                name: comms-secrets
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: comms
spec:
  selector:
    app: comms
  ports:
    - port: 80
      targetPort: 3000
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: comms-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: comms
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: comms-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: comms
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
