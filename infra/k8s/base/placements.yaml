apiVersion: apps/v1
kind: Deployment
metadata:
  name: placements
spec:
  replicas: 2
  selector:
    matchLabels:
      app: placements
  template:
    metadata:
      labels:
        app: placements
    spec:
      containers:
        - name: placements
          image: rvtrust/placements:latest
          envFrom:
            - secretRef:
                name: placements-secrets
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: placements
spec:
  selector:
    app: placements
  ports:
    - port: 80
      targetPort: 3000
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: placements-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: placements
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: placements-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: placements
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
